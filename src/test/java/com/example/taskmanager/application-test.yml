spring:
  datasource:
    url: jdbc:h2:mem:testdb  #En memoria para pruebas
    driver-class-name: org.h2.Driver #Controlador H2
    username: root  
    password: sasa

  jpa:
    hibernate:
      ddl-auto: create-drop #Crear y eliminar esquema para cada prueba
    show-sql: true 
    properties:
      hibernate:
        '[format_sql]': true #Formatear SQL para mejor legibilidad

  h2:
    console:
      enabled: false
